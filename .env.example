# Vercel KV (Redis) for production signaling storage
# These are automatically provided by Vercel when you add a KV store
# No need to set manually - just create a KV store in your Vercel project

# KV_REST_API_URL=
# KV_REST_API_TOKEN=
# KV_REST_API_READ_ONLY_TOKEN=
# KV_URL=
